---
efiBootLoader: "grub"

# Ubuntu kernel/initrd symlink paths (used only by non-GRUB EFI loaders,
# kept correct for consistency).
kernel: "/vmlinuz"
img: "/initrd.img"
timeout: "10"

grubInstall: "grub-install"
grubMkconfig: "grub-mkconfig"
grubCfg: "/boot/grub/grub.cfg"

efiBootloaderId: "ubuntu"
